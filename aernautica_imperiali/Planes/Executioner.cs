namespace aernautica_imperiali{
    public class Executioner : APlane{
        public Executioner(Point p, int structure, int speed, int throttle, int minSpeed, int maxSpeed, int maneuver, int handling, int maxAltitude, int planeValue, bool spin) : base(p, 3, speed, 2, 2, 7, 6, 3, 5, 23, false){
        }
    }
}