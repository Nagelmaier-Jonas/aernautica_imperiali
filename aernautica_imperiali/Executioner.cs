namespace aernautica_imperiali{
    public class Executioner : APlane{
        public Executioner(int x, int y, int z, int structure, int speed, int throttle, int minSpeed, int maxSpeed, int maneuver, int handling, int altitude, int maxAltitude, int planeValue, bool spin) : base(x, y, z, structure, speed, throttle, minSpeed, maxSpeed, maneuver, handling, altitude, maxAltitude, planeValue, spin){
        }
    }
}